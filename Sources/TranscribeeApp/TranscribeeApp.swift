import SwiftUI

@main
struct TranscribeeApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Transcribee")
                .frame(minWidth: 480, minHeight: 320)
        }
    }
}

